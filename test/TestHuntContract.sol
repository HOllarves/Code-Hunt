pragma solidity ^0.4.2;

import "truffle/Assert.sol";
import "truffle/DeployedAddresses.sol";
import "../contracts/Hunt.sol";

contract TestHuntContract {

    // uint public initialBalance = 5 ether;
    // Hunt huntContract =  Hunt(DeployedAddresses.Hunt());
    // bool bountyCreated;
    // uint bountyIssue;
    // uint bountyReward;

    // function beforeEach() public {
    //     bountyCreated = huntContract.createBounty.value(2 ether)("https://github.com/HOllarves/Code-Hunt", 1, 432000);
    // }

    // function beforeEachIssue() public {
    //     bountyIssue = huntContract.getBountyIssue(msg.sender);
    // }

    // function testItStoresABounty() public {
    //     Assert.equal(bountyCreated, bool(true), "It should return true as it created a new bounty");
    // }

    // function testItRetrievesBountyIssue() public {
    //     Assert.equal(uint(bountyIssue), uint(1), "It should match the issueId");
    // }

    // function testItRetrievesBountyReward() public {
    //     bountyReward = huntContract.getBountyReward(msg.sender);
    //     Assert.equal(uint(bountyReward), uint(2000000000000000000));
    // }
}

